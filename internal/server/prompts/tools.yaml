# Public tool descriptions. Input schemas are authoritative for arguments.
# exec_command/write_stdin/apply_patch are the programming core; all others manage the gateway.
tools:
  workspace: 零参数列出已注册 Workspace；用于创建 session 前发现工作空间。
  session: 创建、恢复或关闭 Remote Session；省略 action 默认 open/resume，remote_session_id 存在时恢复原会话，mode 可直接推导 close。
  move_out: 安全移出明确文件、目录或 symlink；prepare 冻结 manifest，用户确认后 submit 仅携带服务端 confirmation_uuid。
  observe: 查看会话、执行 Task、Plan Task、历史或日志；目标唯一时可省略 view，历史通过 cursor 分页，任务日志通过 stdout_offset/stderr_offset 续读。源码差异用 exec_command 运行 git diff。
  progress: 发布用户可见进度与终态；过程仅用于有业务意义的里程碑、等待、阻塞或失败，任务正常完成并准备最终回复前必须用 completed + result 汇报一次最终结果。
  exec_command: 通过 MCPX 原生进程 backend 执行 shell 命令，无需 Codex 可执行文件；读取和搜索源码直接运行 rg/cat/sed。返回合并 stdout/stderr 的 output、exit_code，或仍在运行的整数 session_id；非零退出是命令结果。MCPX 按配置执行权限检查，需确认时由 console 审批。
  write_stdin: 向 exec_command 返回的整数 session_id 写入 chars，或省略 chars 等待新输出；结果使用同一 output/exit_code/session_id 契约。进程句柄与 remote_session_id 不同。
  apply_patch: 通过 MCPX 原生补丁 backend 应用 Codex 格式的原始补丁文本，支持 Add/Update/Delete/Move；路径相对当前 Remote Session 的 Workspace 根目录。MCP 仅用 input 字符串包裹 freeform 补丁；权限和确认由 MCPX 配置及 console 控制。
  plan: 创建、读取、推进、阻塞、重规划或交付持久化计划。
  operation_batch: 按 DAG 提交多个网关管理面操作；无依赖步骤可并发。exec_command/write_stdin/apply_patch 直接调用，不通过 Operation 包装。
  operation_manage: 查询、等待、读取结果、取消或恢复异步 Operation。
  runtime_read: 读取 MCPX 运行时能力、项目摘要或适用指令；project/instructions 可指定 workspace，无需先建 session；若同时提供 remote_session_id，必须与 workspace 一致。
  environment_read: 读取当前主机/工具链环境或与已保存快照比较。
  environment: 保存当前环境快照。
  skill_tool: 统一完成 Skill 的 list、describe 与 call；Runtime 内部负责 revision 一致性。
  mcp_tool: 统一完成 MCP Server/Tool 的 list、describe 与 call；Runtime 内部负责 schema revision 一致性。
  browser: 通过本机 OpenAI 官方 ChatGPT/Codex 浏览器扩展复用现有 Chrome/Edge 与登录态；提供常用标签页/导航/DOM-CUA/输入/截图强类型动作，并以 official 高级入口覆盖当前 Browser Service 的 Playwright/AX/WebMCP/history/export/dialog 等 execute 命令；所有页面访问和敏感动作保留官方 Browser Service 安全检查及 user_confirmed 两阶段确认。
  artifact: 登记、列出或分片读取 MCPX 开发产物。
  screenshot_capture: 按明确 purpose 截取显示器或屏幕区域。
  secret_provide: 按明确 purpose 向当前会话提供仅驻留内存的 Secret。
